services:
  game-service:
    environment:
      GAME_DB_PATH: ":memory:"
      NODE_ENV: test

  test-runner:
    image: node:20-alpine
    working_dir: /repo
    volumes:
      - ..:/repo
    environment:
      NODE_ENV: test
    command: sh -lc "npm ci && npm run test --workspace srcs/users"
